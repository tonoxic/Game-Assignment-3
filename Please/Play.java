import javax.swing.JFrame;

// Main function
// - runs the game
public class Play {
    public static void main(String [] args) {
        // Initialise the GameView to start the game
        JFrame gv = GameView.getInstance();
    }
}
